name: phoenix_core
description: Phoenix core — domain, config, logging, DI. Zero UI dependencies.
version: 0.1.0-alpha
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  meta: ^1.16.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
