{
  "name": "@phoenix/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "season": "tsx src/main.ts",
    "lint": "eslint src"
  },
  "dependencies": {
    "@phoenix/simulation": "workspace:*",
    "@phoenix/contracts": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
