name: phoenix_tools
description: Phoenix developer tools — Simulation Lab, inspectors (dev-only).
version: 0.1.0-alpha
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  meta: ^1.16.0
  phoenix_core:
    path: ../phoenix_core
  phoenix_engine:
    path: ../phoenix_engine

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
